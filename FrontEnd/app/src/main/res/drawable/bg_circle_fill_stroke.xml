<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- 내부 색은 코드에서 tint로 칠할 거라 투명 -->
    <solid android:color="@android:color/transparent" />

    <!-- 비선택도 항상 보이는 검은 테두리 -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black" />
</shape>
