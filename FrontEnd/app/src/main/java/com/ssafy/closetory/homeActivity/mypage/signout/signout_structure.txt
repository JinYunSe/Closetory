- 구조 (기능 별로 구조 상이함)
    com.ssafy.closetory
    ├─ homeActivity
    │  └─ mypage
    │     ├─ MyPageFragment.kt
    │     │  └─ 마이페이지 메인 화면, 로그아웃 / 회원 탈퇴 진입
    │     │
    │     └─ signout
    │        ├─ SignoutFragment.kt
    │        │  └─ 회원 탈퇴 다이얼로그 UI 표시, 비밀번호 입력 처리
    │        │
    │        ├─ SignoutViewModel.kt
    │        │  └─ 회원 탈퇴 로직 처리, 서버 응답 결과 관리
    │        │
    │        ├─ SignoutRepository.kt
    │        │  └─ 회원 탈퇴 관련 데이터 요청 중계 (Service ↔ ViewModel)
    │        │
    │        └─ SignoutService.kt
    │           └─ 회원 탈퇴 API 정의 (DELETE /users/{userId})
    │
    ├─ dto
    │  ├─ SignoutRequest.kt
    │  │  └─ 회원 탈퇴 요청 데이터 모델 (비밀번호)
    │  │
    │  └─ SignoutResponse.kt
    │     └─ 회원 탈퇴 응답 데이터 모델 (ApiResponse<Unit>)
    │
    └─ res
       └─ layout
          ├─ fragment_my_page.xml
          │  └─ 마이페이지 메인 UI 레이아웃
          │
          └─ dialog_signout.xml
             └─ 회원 탈퇴 비밀번호 입력용 AlertDialog 레이아웃


--- 기본적인 작성 틀 ---
- 작성 순서
    Request / Response → Service → Repository → ViewModel → Fragment

- 기능별 간단한 주석 추가. (이모지 사용 금지.)

- ApiRespone<T> 사용.
    data class ApiResponse<T>(
        val httpStatusCode: Int,
        val responseMessage: String?,
        val errorMessage: String?,
        val data: T?
    )

- getUserId의 경우 SharedPreferencesUtil에 구현 / ApplicationClass에 USERID 구현
    val userId = ApplicationClass.sharedPreferences.getUserId(ApplicationClass.USERID) ?: return
    const val USERID = "userId"


- AuthInterceptor.kt 사용.
    package com.ssafy.closetory.util

    import android.util.Log
    import com.ssafy.closetory.ApplicationClass
    import okhttp3.Interceptor
    import okhttp3.Response

    // 모든 요청에 대해 해더에 token 넣기
    private const val TAG = "AuthInterceptor_싸피"

    // 요청에 Bearer 토큰 넣는 해더
    class AuthInterceptor : okhttp3.Interceptor {
        override fun intercept(chain: Interceptor.Chain): Response {
            val req = chain.request()
            val path = req.url.encodedPath

            // 토큰 필요 없는 주소
            val noAuth = listOf("/auth/signup")

            if (noAuth.any { path.contains(it) }) return chain.proceed(req)

            val token = ApplicationClass.authManager.getAccessToken()

            // 토큰이 있는 상황이면
            val newRequest = if (!token.isNullOrEmpty()) {
                // 해더에 Bearer token 값 넣기
                req.newBuilder()
                    .header(ApplicationClass.X_ACCESS_TOKEN, "Bearer $token")
                    .build()
            } else {
                req
            }

            // log로 request할 때 토큰이 잘 가는지 확인하기
            Log.d(TAG, "url=${newRequest.url}")
            Log.d(TAG, "Authorization: ${newRequest.header("Authorization")}")

            return chain.proceed(newRequest)
        }
    }

- 통신 결과가 한 번만 이루어지는 것들 -> MutableLiveData (X) -> MUtableSharedFlow (O)
  => message.value -> message.emit
  => Fragment에서 viewLifeCtcleOwner.lifecyclesScope.lauch{} 로 한 번 감싸줘야한다.


- 프론트에서 사용자 입력 검증을 하는 것은 Fragment에서 진행 (ViewModel에서 중복 X)

- ViewModel의 경우 Retrofit Response를 토대로 작성

- xml에서 엔터를 치면 두 줄로 넘어가면 안된다. 다음 태그로 넘어가야한다.

- 비밀번호의 경우 하단과 같이 비밀번호 보기/숨기기 표시를 추가한다.
	<LinearLayout
       	    android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="16dp"
            android:orientation="horizontal"
            android:background="#E0E0E0"
            android:gravity="center_vertical">

        <EditText
            android:id="@+id/etLoginPassword"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:hint="비밀번호"
            android:inputType="textPassword"
            android:background="@null"
            android:padding="16dp" />

        <ImageButton
            android:id="@+id/btnToggleLoginPassword"
            android:layout_width="48dp"
            android:layout_height="match_parent"
            android:background="@null"
            android:src="@drawable/ic_eye"
            android:contentDescription="비밀번호 보기/숨기기" />

    </LinearLayout>

- 다이얼로그 구현은 Fragment 내에서 구현
	private fun showChangePasswordDialog() {
        	val dialogView = layoutInflater
            	.inflate(R.layout.dialog_edit_profile_password, null)

- 생성된 Retrofit 이용.
    ApplicationClass.retrofit : 이미 생성된 Retrofit 인스턴스
    .create(SignoutService::class.java) : 구현체 생성
    Repository가 Service를 직접 생성해서 사용